name: location
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  video_player: ^2.9.1
  google_maps_flutter: ^2.7.0
  geolocator: ^14.0.2          # ✅ อัปเดตล่าสุด
  permission_handler: ^12.0.1
  image_picker: ^1.1.2
  shared_preferences: ^2.3.2
  geocoding: ^4.0.0            # ✅ อัปเดตให้รองรับ Flutter 3.8
  xml: ^6.5.0
  mqtt_client: ^10.2.1
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
    - assets/sea_drive.mp4
    - assets/thai_flag.png
    - assets/map_styles/forest_light.json
    